# Metaspace Mid-Tier
This tier offers a WASM API to the Presentation Tier and depends upon API's provided by the Metaspace Storage Tier. Although executing within a holochain Conductor continer, the code in this tier should not expose on holochain dependencies. 

